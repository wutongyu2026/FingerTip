# FingerTip-Engine 基础依赖
# 仅 mock 模式运行所需的最小集（测试用 fastapi.testclient，无需起 uvicorn）
# 真实后端（llama-cpp-python / sd-cpp / step-audio）需用户自行安装，不写在此处——见 README

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0

# 测试依赖
pytest>=8,<10
httpx>=0.27,<1.0
